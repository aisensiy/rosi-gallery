from setuptools import setup

requires = [
    'flask',
    'PIL',
    'BeautifulSoup',
    'requests'
]

setup(name='rosi-view',
      version='0.1',
      author='aisensiy',
      author_email='aisensiy@gmail.com',
      install_requires=requires)
